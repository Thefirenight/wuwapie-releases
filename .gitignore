# Sorties de build — non versionnées (uploadées comme assets GitHub Release)
WuwaPie/
WuwaPieLauncher/
WuwaDatabase.db
*.zip

# Script de packaging local
pack.ps1

# Versionnés dans ce repo
!manifest.json
!assets/

# Electron installateur — dépendances et sorties de build
Setup/node_modules/
Setup/dist/
Setup/payload/*.msix
Setup/payload/*.exe
Setup/.env
